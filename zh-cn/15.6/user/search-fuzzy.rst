======
模糊检索
======

模糊检索(Fuzzy检索)
=================

当您想搜索与搜索词不完全匹配的单词时,可以使用模糊检索。|Fess| 支持基于莱文斯坦距离的模糊检索(Fuzzy 检索)。

使用方法
------

在要应用模糊检索的搜索词后添加"~"。

例如,如果想对"Fess"这个单词进行模糊检索,通过在搜索表单中输入如下内容,可以搜索包含与"Fess"接近的词(如"Fes"等)的文档。

::

    Fess~
