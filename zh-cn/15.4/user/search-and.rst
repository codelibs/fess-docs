=======
AND 检索
=======

当您想搜索包含多个搜索词的文档时,可以使用 AND 检索。如果省略 AND 并在搜索输入字段中以空格分隔输入多个词,也会执行 AND 检索。

使用方法
------

使用 AND 检索时,在搜索词之间输入 AND。AND
必须使用半角大写字母,前后需要空格。
也可以省略 AND。

例如,如果想搜索包含"搜索词1"和"搜索词2"的文档,在搜索表单中输入如下内容:

::

    搜索词1 AND 搜索词2

也可以用 AND 连接多个词。
